{
    "tasks": {
      "test": "deno test --allow-all --coverage=coverage",
      "test:dev": "deno test --allow-all --watch",
      "lint": "deno lint",
      "fmt": "deno fmt"
    },
    "lint": {
      "rules": {
        "tags": ["recommended"]
      }
    },
    "fmt": {
      "files": {
        "exclude": ["**/README.md"]
      }
    }
  }
  